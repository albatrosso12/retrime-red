# Frontend Environment Variables (Cloudflare Pages)
# Only the Worker URL is needed - Discord OAuth is handled by Worker

# API Backend (Cloudflare Worker URL)
VITE_API_URL=https://retrime.korsetov2009.workers.dev
